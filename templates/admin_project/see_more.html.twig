{% extends 'base.html.twig' %}

{% block title %}Omera Digital project: {{ project.subject }}{% endblock %}

{% block body %}
  <div class="row">
    <div class="col-md-4">
      <h3 class="h4">Challenge</h3>
      <p class="text-center">{{ project.challenge }}</p>
    </div>
    <div class="col-md-4">
      <h3 class="h4">Solution</h3>
      <p class="text-center">{{ project.solution }}</p>
    </div>
    <div class="col-md-4 jumbotron">
      <p>Client: {{ project.customer }}</p>
      <p>Budget: {{ project.budget|number_format(0, ' ', ' ') }} FCFA</p>
      <p>Categories: {{ project.getCategoriesNames }}</p>
      <p>Date: {{ project.createdAt|date('Y-m-d') }}</p>
    </div>
  </div>
  <div class="row">
    <img src="/uploads/img/project/{{ project.images.first.url }}" alt="" class="img-fluid" height="300px">
  </div>
  <div class="row">
    <div class="col-md-6 bg-danger">
      <h4 class="h4 text-white">Landing Page Deisgn</h4>
      <p class="text-center">
        This communication platform makes it easy to share updates from any classroom around the world, empowering teachers, parents and students to connect and learn. Having reached more than 180 countries, it's clearly projecting.To maintain growth however, the company had to ensure its app was best-in-class with a growing community.
      </p>
    </div>
    <div class="col-md-6">
      <img src="/uploads/img/project/{{ project.images.first.url }}" alt="" class="img-fuild" height="300px" width="400px">
    </div>
  </div>
{% endblock %}
